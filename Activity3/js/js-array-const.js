// Create an Array:
const letter = ["X", "B", "C"];

// Change an element:
letter[0] = "A";

// Add an element:
letter.push("D");

// Display the Array:
document.getElementById("aconst").innerHTML = letter; 