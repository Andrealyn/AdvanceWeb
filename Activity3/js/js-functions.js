var x2 = myFunction(3, 674); // Function is called, return value will end up in x
document.getElementById("functions").innerHTML = x2;

function myFunction(a2, b2) {
  return a2 * b2; // Function returns the product of a and b
}